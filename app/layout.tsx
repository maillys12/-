import type {Metadata} from "next";import "./globals.css";export const metadata:Metadata={title:"MONTYSTORE | แอปพรีเมียม",description:"เลือกซื้อแอปพรีเมียม สะดวก รวดเร็ว พร้อมดูแลทุกออเดอร์",icons:{icon:"/favicon.svg"}};export default function RootLayout({children}:Readonly<{children:React.ReactNode}>){return <html lang="th"><body>{children}</body></html>}
